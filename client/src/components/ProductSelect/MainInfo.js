import React from 'react';
import PropTypes from 'prop-types';

const MainInfo = (props) => {
  return <div>MAIN .indexOf(searchElement, fromIndex)</div>;
};

MainInfo.propTypes = {};

export default MainInfo;
